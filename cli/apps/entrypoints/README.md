# Entrypoints CLI App

Command-line app for managing applications, templates, and configuration across the monorepo.

This app is executed through the top-level repo CLI:

```bash
bg entrypoints <command>
```

## Quick Start

```bash
# List all applications
bg entrypoints list

# Sync configuration across all apps
bg entrypoints sync

# Get help
bg entrypoints help
```
