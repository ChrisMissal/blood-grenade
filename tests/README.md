# tests

This folder contains integration tests for the CLI. Each test case should have:
- An input command (as text or file)
- An expected output (as text or file)

Example structure:
- test-case-1/
  - input.txt
  - expected.txt
