# Filesystem Integration

The default implementation currently lives in `apps/cli/src/integrations/filesystem/inspector.ts`.
This namespace is kept as a top-level documentation anchor for integration boundaries.
